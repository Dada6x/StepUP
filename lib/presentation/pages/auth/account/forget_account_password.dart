import 'package:flutter/material.dart';

class ForgetAccountPassword extends StatelessWidget {
  const ForgetAccountPassword({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}