import 'package:flutter/material.dart';

class StartUpDashbord extends StatelessWidget {
  const StartUpDashbord({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}