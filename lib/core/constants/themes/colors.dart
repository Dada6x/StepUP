import 'package:flutter/material.dart';

// TODO add the app colors later
class AppColors {
  static const primary = Color(0xFF0088FF);
  static const danger = Color(0xFFFA4549);
  static const success = Color(0xFF34D058);
  static const background = Color(0xFFF7F8FA);
}
